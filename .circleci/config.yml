version: '2.1'
orbs:
  snyk: snyk/snyk@1.2.3
  node: circleci/node@5.0.2
jobs:
  build:
    docker:
      - image: 'cimg/node:lts'
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: |
            npm install --save
      - snyk/scan:
          fail-on-issues: false
workflows: null